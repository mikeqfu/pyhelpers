coverage:
  status:
    project:
      default:
        target: 80%        # mirrors [tool.coverage.report] fail_under
        threshold: 1%      # tolerate tiny fluctuations
    patch:
      default:
        informational: true
comment:
  layout: "condensed"
